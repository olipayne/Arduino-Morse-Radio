#include "Stations.h"

// Define the stations
Station stations[] = {
    {"London", 1000},    // Replace with actual ADC values
    {"Hilversum", 2000}, // Replace with actual ADC values
    {"Barcelona", 3000}  // Replace with actual ADC values
};

const int numStations = sizeof(stations) / sizeof(stations[0]);
